The WDL you just generated failed validation with these errors from `sprocket check`:

{{stderr}}

Please regenerate the WDL with these errors fixed. Keep everything else the same. Respond only with the corrected WDL inside a ```wdl code block.
